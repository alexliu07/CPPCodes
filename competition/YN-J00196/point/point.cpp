#include <bits/stdc++.h>

using namespace std;

int main() {
//	freopen("point.in", "r", stdin);
//	freopen("point.out", "w", stdout);
    int n, k, points[500][2], countx = 0, county = 0;
    cin >> n >> k;
    for (int i = 0; i < n; i++) {
        points[i][0] = 0;
        points[i][1] = 0;
        cin >> points[i][0] >> points[i][1];
    }

    return 0;
}