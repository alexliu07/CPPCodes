//
// Created by liuyu on 2023/2/26.
//

#include <bits/stdc++.h>

#define _for(i, a, b) for(int i=(a);i>=(b);i--)

using namespace std;

int main() {
    string a;
    cin >> a;
    _for(i, a.length() - 1, 0)cout << a[i];
    return 0;
}
